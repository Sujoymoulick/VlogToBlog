{
  "name": "vlogtoblog-monorepo",
  "version": "1.0.0",
  "description": "VlogToBlog Monorepo root",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "backend": "npm start --prefix backend",
    "frontend": "npm run dev --prefix frontend"
  }
}
